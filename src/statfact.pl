:- module(statfact,[]).
have_base_stat(bulbasaur,318).
have_base_stat(ivysaur,405).
have_base_stat(venusaur,525).
have_base_stat(charmander,309).
have_base_stat(charmeleon,405).
have_base_stat(charizard,534).
have_base_stat(squirtle,314).
have_base_stat(wartortle,405).
have_base_stat(blastoise,530).
have_base_stat(caterpie,195).
have_base_stat(metapod,205).
have_base_stat(butterfree,395).
have_base_stat(weedle,195).
have_base_stat(kakuna,205).
have_base_stat(beedrill,395).
have_base_stat(pidgey,251).
have_base_stat(pidgeotto,349).
have_base_stat(pidgeot,479).
have_base_stat(rattata,253).
have_base_stat(raticate,413).
have_base_stat(spearow,262).
have_base_stat(fearow,442).
have_base_stat(ekans,288).
have_base_stat(arbok,438).
have_base_stat(pikachu,320).
have_base_stat(raichu,485).
have_base_stat(sandshrew,300).
have_base_stat(sandslash,450).
have_base_stat(nidoran-f,275).
have_base_stat(nidorina,365).
have_base_stat(nidoqueen,505).
have_base_stat(nidoran-m,273).
have_base_stat(nidorino,365).
have_base_stat(nidoking,505).
have_base_stat(clefairy,323).
have_base_stat(clefable,483).
have_base_stat(vulpix,299).
have_base_stat(ninetales,505).
have_base_stat(jigglypuff,270).
have_base_stat(wigglytuff,435).
have_base_stat(zubat,245).
have_base_stat(golbat,455).
have_base_stat(oddish,320).
have_base_stat(gloom,395).
have_base_stat(vileplume,490).
have_base_stat(paras,285).
have_base_stat(parasect,405).
have_base_stat(venonat,305).
have_base_stat(venomoth,450).
have_base_stat(diglett,265).
have_base_stat(dugtrio,405).
have_base_stat(meowth,290).
have_base_stat(persian,440).
have_base_stat(psyduck,320).
have_base_stat(golduck,500).
have_base_stat(mankey,305).
have_base_stat(primeape,455).
have_base_stat(growlithe,350).
have_base_stat(arcanine,555).
have_base_stat(poliwag,300).
have_base_stat(poliwhirl,385).
have_base_stat(poliwrath,510).
have_base_stat(abra,310).
have_base_stat(kadabra,400).
have_base_stat(alakazam,500).
have_base_stat(machop,305).
have_base_stat(machoke,405).
have_base_stat(machamp,505).
have_base_stat(bellsprout,300).
have_base_stat(weepinbell,390).
have_base_stat(victreebel,490).
have_base_stat(tentacool,335).
have_base_stat(tentacruel,515).
have_base_stat(geodude,300).
have_base_stat(graveler,390).
have_base_stat(golem,495).
have_base_stat(ponyta,410).
have_base_stat(rapidash,500).
have_base_stat(slowpoke,315).
have_base_stat(slowbro,490).
have_base_stat(magnemite,325).
have_base_stat(magneton,465).
have_base_stat(farfetchd,352).
have_base_stat(doduo,310).
have_base_stat(dodrio,460).
have_base_stat(seel,325).
have_base_stat(dewgong,475).
have_base_stat(grimer,325).
have_base_stat(muk,500).
have_base_stat(shellder,305).
have_base_stat(cloyster,525).
have_base_stat(gastly,310).
have_base_stat(haunter,405).
have_base_stat(gengar,500).
have_base_stat(onix,385).
have_base_stat(drowzee,328).
have_base_stat(hypno,483).
have_base_stat(krabby,325).
have_base_stat(kingler,475).
have_base_stat(voltorb,330).
have_base_stat(electrode,480).
have_base_stat(exeggcute,325).
have_base_stat(exeggutor,520).
have_base_stat(cubone,320).
have_base_stat(marowak,425).
have_base_stat(hitmonlee,455).
have_base_stat(hitmonchan,455).
have_base_stat(lickitung,385).
have_base_stat(koffing,340).
have_base_stat(weezing,490).
have_base_stat(rhyhorn,345).
have_base_stat(rhydon,485).
have_base_stat(chansey,450).
have_base_stat(tangela,435).
have_base_stat(kangaskhan,490).
have_base_stat(horsea,295).
have_base_stat(seadra,440).
have_base_stat(goldeen,320).
have_base_stat(seaking,450).
have_base_stat(staryu,340).
have_base_stat(starmie,520).
have_base_stat(mr-mime,460).
have_base_stat(scyther,500).
have_base_stat(jynx,455).
have_base_stat(electabuzz,490).
have_base_stat(magmar,495).
have_base_stat(pinsir,500).
have_base_stat(tauros,490).
have_base_stat(magikarp,200).
have_base_stat(gyarados,540).
have_base_stat(lapras,535).
have_base_stat(ditto,288).
have_base_stat(eevee,325).
have_base_stat(vaporeon,525).
have_base_stat(jolteon,525).
have_base_stat(flareon,525).
have_base_stat(porygon,395).
have_base_stat(omanyte,355).
have_base_stat(omastar,495).
have_base_stat(kabuto,355).
have_base_stat(kabutops,495).
have_base_stat(aerodactyl,515).
have_base_stat(snorlax,540).
have_base_stat(articuno,580).
have_base_stat(zapdos,580).
have_base_stat(moltres,580).
have_base_stat(dratini,300).
have_base_stat(dragonair,420).
have_base_stat(dragonite,600).
have_base_stat(mewtwo,680).
have_base_stat(mew,600).
have_base_stat(chikorita,318).
have_base_stat(bayleef,405).
have_base_stat(meganium,525).
have_base_stat(cyndaquil,309).
have_base_stat(quilava,405).
have_base_stat(typhlosion,534).
have_base_stat(totodile,314).
have_base_stat(croconaw,405).
have_base_stat(feraligatr,530).
have_base_stat(sentret,215).
have_base_stat(furret,415).
have_base_stat(hoothoot,262).
have_base_stat(noctowl,442).
have_base_stat(ledyba,265).
have_base_stat(ledian,390).
have_base_stat(spinarak,250).
have_base_stat(ariados,390).
have_base_stat(crobat,535).
have_base_stat(chinchou,330).
have_base_stat(lanturn,460).
have_base_stat(pichu,205).
have_base_stat(cleffa,218).
have_base_stat(igglybuff,210).
have_base_stat(togepi,245).
have_base_stat(togetic,405).
have_base_stat(natu,320).
have_base_stat(xatu,470).
have_base_stat(mareep,280).
have_base_stat(flaaffy,365).
have_base_stat(ampharos,510).
have_base_stat(bellossom,490).
have_base_stat(marill,250).
have_base_stat(azumarill,420).
have_base_stat(sudowoodo,410).
have_base_stat(politoed,500).
have_base_stat(hoppip,250).
have_base_stat(skiploom,340).
have_base_stat(jumpluff,460).
have_base_stat(aipom,360).
have_base_stat(sunkern,180).
have_base_stat(sunflora,425).
have_base_stat(yanma,390).
have_base_stat(wooper,210).
have_base_stat(quagsire,430).
have_base_stat(espeon,525).
have_base_stat(umbreon,525).
have_base_stat(murkrow,405).
have_base_stat(slowking,490).
have_base_stat(misdreavus,435).
have_base_stat(unown,336).
have_base_stat(wobbuffet,405).
have_base_stat(girafarig,455).
have_base_stat(pineco,290).
have_base_stat(forretress,465).
have_base_stat(dunsparce,415).
have_base_stat(gligar,430).
have_base_stat(steelix,510).
have_base_stat(snubbull,300).
have_base_stat(granbull,450).
have_base_stat(qwilfish,430).
have_base_stat(scizor,500).
have_base_stat(shuckle,505).
have_base_stat(heracross,500).
have_base_stat(sneasel,430).
have_base_stat(teddiursa,330).
have_base_stat(ursaring,500).
have_base_stat(slugma,250).
have_base_stat(magcargo,410).
have_base_stat(swinub,250).
have_base_stat(piloswine,450).
have_base_stat(corsola,380).
have_base_stat(remoraid,300).
have_base_stat(octillery,480).
have_base_stat(delibird,330).
have_base_stat(mantine,465).
have_base_stat(skarmory,465).
have_base_stat(houndour,330).
have_base_stat(houndoom,500).
have_base_stat(kingdra,540).
have_base_stat(phanpy,330).
have_base_stat(donphan,500).
have_base_stat(porygon2,515).
have_base_stat(stantler,465).
have_base_stat(smeargle,250).
have_base_stat(tyrogue,210).
have_base_stat(hitmontop,455).
have_base_stat(smoochum,305).
have_base_stat(elekid,360).
have_base_stat(magby,365).
have_base_stat(miltank,490).
have_base_stat(blissey,540).
have_base_stat(raikou,580).
have_base_stat(entei,580).
have_base_stat(suicune,580).
have_base_stat(larvitar,300).
have_base_stat(pupitar,410).
have_base_stat(tyranitar,600).
have_base_stat(lugia,680).
have_base_stat(ho-oh,680).
have_base_stat(celebi,600).
have_base_stat(treecko,310).
have_base_stat(grovyle,405).
have_base_stat(sceptile,530).
have_base_stat(torchic,310).
have_base_stat(combusken,405).
have_base_stat(blaziken,530).
have_base_stat(mudkip,310).
have_base_stat(marshtomp,405).
have_base_stat(swampert,535).
have_base_stat(poochyena,220).
have_base_stat(mightyena,420).
have_base_stat(zigzagoon,240).
have_base_stat(linoone,420).
have_base_stat(wurmple,195).
have_base_stat(silcoon,205).
have_base_stat(beautifly,395).
have_base_stat(cascoon,205).
have_base_stat(dustox,385).
have_base_stat(lotad,220).
have_base_stat(lombre,340).
have_base_stat(ludicolo,480).
have_base_stat(seedot,220).
have_base_stat(nuzleaf,340).
have_base_stat(shiftry,480).
have_base_stat(taillow,270).
have_base_stat(swellow,430).
have_base_stat(wingull,270).
have_base_stat(pelipper,430).
have_base_stat(ralts,198).
have_base_stat(kirlia,278).
have_base_stat(gardevoir,518).
have_base_stat(surskit,269).
have_base_stat(masquerain,414).
have_base_stat(shroomish,295).
have_base_stat(breloom,460).
have_base_stat(slakoth,280).
have_base_stat(vigoroth,440).
have_base_stat(slaking,670).
have_base_stat(nincada,266).
have_base_stat(ninjask,456).
have_base_stat(shedinja,236).
have_base_stat(whismur,240).
have_base_stat(loudred,360).
have_base_stat(exploud,490).
have_base_stat(makuhita,237).
have_base_stat(hariyama,474).
have_base_stat(azurill,190).
have_base_stat(nosepass,375).
have_base_stat(skitty,260).
have_base_stat(delcatty,380).
have_base_stat(sableye,380).
have_base_stat(mawile,380).
have_base_stat(aron,330).
have_base_stat(lairon,430).
have_base_stat(aggron,530).
have_base_stat(meditite,280).
have_base_stat(medicham,410).
have_base_stat(electrike,295).
have_base_stat(manectric,475).
have_base_stat(plusle,405).
have_base_stat(minun,405).
have_base_stat(volbeat,400).
have_base_stat(illumise,400).
have_base_stat(roselia,400).
have_base_stat(gulpin,302).
have_base_stat(swalot,467).
have_base_stat(carvanha,305).
have_base_stat(sharpedo,460).
have_base_stat(wailmer,400).
have_base_stat(wailord,500).
have_base_stat(numel,305).
have_base_stat(camerupt,460).
have_base_stat(torkoal,470).
have_base_stat(spoink,330).
have_base_stat(grumpig,470).
have_base_stat(spinda,360).
have_base_stat(trapinch,290).
have_base_stat(vibrava,340).
have_base_stat(flygon,520).
have_base_stat(cacnea,335).
have_base_stat(cacturne,475).
have_base_stat(swablu,310).
have_base_stat(altaria,490).
have_base_stat(zangoose,458).
have_base_stat(seviper,458).
have_base_stat(lunatone,440).
have_base_stat(solrock,440).
have_base_stat(barboach,288).
have_base_stat(whiscash,468).
have_base_stat(corphish,308).
have_base_stat(crawdaunt,468).
have_base_stat(baltoy,300).
have_base_stat(claydol,500).
have_base_stat(lileep,355).
have_base_stat(cradily,495).
have_base_stat(anorith,355).
have_base_stat(armaldo,495).
have_base_stat(feebas,200).
have_base_stat(milotic,540).
have_base_stat(castform,420).
have_base_stat(kecleon,440).
have_base_stat(shuppet,295).
have_base_stat(banette,455).
have_base_stat(duskull,295).
have_base_stat(dusclops,455).
have_base_stat(tropius,460).
have_base_stat(chimecho,425).
have_base_stat(absol,465).
have_base_stat(wynaut,260).
have_base_stat(snorunt,300).
have_base_stat(glalie,480).
have_base_stat(spheal,290).
have_base_stat(sealeo,410).
have_base_stat(walrein,530).
have_base_stat(clamperl,345).
have_base_stat(huntail,485).
have_base_stat(gorebyss,485).
have_base_stat(relicanth,485).
have_base_stat(luvdisc,330).
have_base_stat(bagon,300).
have_base_stat(shelgon,420).
have_base_stat(salamence,600).
have_base_stat(beldum,300).
have_base_stat(metang,420).
have_base_stat(metagross,600).
have_base_stat(regirock,580).
have_base_stat(regice,580).
have_base_stat(registeel,580).
have_base_stat(latias,600).
have_base_stat(latios,600).
have_base_stat(kyogre,670).
have_base_stat(groudon,670).
have_base_stat(rayquaza,680).
have_base_stat(jirachi,600).
have_base_stat(deoxys-normal,600).
have_base_stat(turtwig,318).
have_base_stat(grotle,405).
have_base_stat(torterra,525).
have_base_stat(chimchar,309).
have_base_stat(monferno,405).
have_base_stat(infernape,534).
have_base_stat(piplup,314).
have_base_stat(prinplup,405).
have_base_stat(empoleon,530).
have_base_stat(starly,245).
have_base_stat(staravia,340).
have_base_stat(staraptor,485).
have_base_stat(bidoof,250).
have_base_stat(bibarel,410).
have_base_stat(kricketot,194).
have_base_stat(kricketune,384).
have_base_stat(shinx,263).
have_base_stat(luxio,363).
have_base_stat(luxray,523).
have_base_stat(budew,280).
have_base_stat(roserade,515).
have_base_stat(cranidos,350).
have_base_stat(rampardos,495).
have_base_stat(shieldon,350).
have_base_stat(bastiodon,495).
have_base_stat(burmy,224).
have_base_stat(wormadam-plant,424).
have_base_stat(mothim,424).
have_base_stat(combee,244).
have_base_stat(vespiquen,474).
have_base_stat(pachirisu,405).
have_base_stat(buizel,330).
have_base_stat(floatzel,495).
have_base_stat(cherubi,275).
have_base_stat(cherrim,450).
have_base_stat(shellos,325).
have_base_stat(gastrodon,475).
have_base_stat(ambipom,482).
have_base_stat(drifloon,348).
have_base_stat(drifblim,498).
have_base_stat(buneary,350).
have_base_stat(lopunny,480).
have_base_stat(mismagius,495).
have_base_stat(honchkrow,505).
have_base_stat(glameow,310).
have_base_stat(purugly,452).
have_base_stat(chingling,285).
have_base_stat(stunky,329).
have_base_stat(skuntank,479).
have_base_stat(bronzor,300).
have_base_stat(bronzong,500).
have_base_stat(bonsly,290).
have_base_stat(mime-jr,310).
have_base_stat(happiny,220).
have_base_stat(chatot,411).
have_base_stat(spiritomb,485).
have_base_stat(gible,300).
have_base_stat(gabite,410).
have_base_stat(garchomp,600).
have_base_stat(munchlax,390).
have_base_stat(riolu,285).
have_base_stat(lucario,525).
have_base_stat(hippopotas,330).
have_base_stat(hippowdon,525).
have_base_stat(skorupi,330).
have_base_stat(drapion,500).
have_base_stat(croagunk,300).
have_base_stat(toxicroak,490).
have_base_stat(carnivine,454).
have_base_stat(finneon,330).
have_base_stat(lumineon,460).
have_base_stat(mantyke,345).
have_base_stat(snover,334).
have_base_stat(abomasnow,494).
have_base_stat(weavile,510).
have_base_stat(magnezone,535).
have_base_stat(lickilicky,515).
have_base_stat(rhyperior,535).
have_base_stat(tangrowth,535).
have_base_stat(electivire,540).
have_base_stat(magmortar,540).
have_base_stat(togekiss,545).
have_base_stat(yanmega,515).
have_base_stat(leafeon,525).
have_base_stat(glaceon,525).
have_base_stat(gliscor,510).
have_base_stat(mamoswine,530).
have_base_stat(porygon-z,535).
have_base_stat(gallade,518).
have_base_stat(probopass,525).
have_base_stat(dusknoir,525).
have_base_stat(froslass,480).
have_base_stat(rotom,440).
have_base_stat(uxie,580).
have_base_stat(mesprit,580).
have_base_stat(azelf,580).
have_base_stat(dialga,680).
have_base_stat(palkia,680).
have_base_stat(heatran,600).
have_base_stat(regigigas,670).
have_base_stat(giratina-altered,680).
have_base_stat(cresselia,600).
have_base_stat(phione,480).
have_base_stat(manaphy,600).
have_base_stat(darkrai,600).
have_base_stat(shaymin-land,600).
have_base_stat(arceus,720).
have_base_stat(victini,600).
have_base_stat(snivy,308).
have_base_stat(servine,413).
have_base_stat(serperior,528).
have_base_stat(tepig,308).
have_base_stat(pignite,418).
have_base_stat(emboar,528).
have_base_stat(oshawott,308).
have_base_stat(dewott,413).
have_base_stat(samurott,528).
have_base_stat(patrat,255).
have_base_stat(watchog,420).
have_base_stat(lillipup,275).
have_base_stat(herdier,370).
have_base_stat(stoutland,500).
have_base_stat(purrloin,281).
have_base_stat(liepard,446).
have_base_stat(pansage,316).
have_base_stat(simisage,498).
have_base_stat(pansear,316).
have_base_stat(simisear,498).
have_base_stat(panpour,316).
have_base_stat(simipour,498).
have_base_stat(munna,292).
have_base_stat(musharna,487).
have_base_stat(pidove,264).
have_base_stat(tranquill,358).
have_base_stat(unfezant,488).
have_base_stat(blitzle,295).
have_base_stat(zebstrika,497).
have_base_stat(roggenrola,280).
have_base_stat(boldore,390).
have_base_stat(gigalith,515).
have_base_stat(woobat,313).
have_base_stat(swoobat,425).
have_base_stat(drilbur,328).
have_base_stat(excadrill,508).
have_base_stat(audino,445).
have_base_stat(timburr,305).
have_base_stat(gurdurr,405).
have_base_stat(conkeldurr,505).
have_base_stat(tympole,294).
have_base_stat(palpitoad,384).
have_base_stat(seismitoad,509).
have_base_stat(throh,465).
have_base_stat(sawk,465).
have_base_stat(sewaddle,310).
have_base_stat(swadloon,380).
have_base_stat(leavanny,500).
have_base_stat(venipede,260).
have_base_stat(whirlipede,360).
have_base_stat(scolipede,485).
have_base_stat(cottonee,280).
have_base_stat(whimsicott,480).
have_base_stat(petilil,280).
have_base_stat(lilligant,480).
have_base_stat(basculin-red-striped,460).
have_base_stat(sandile,292).
have_base_stat(krokorok,351).
have_base_stat(krookodile,519).
have_base_stat(darumaka,315).
have_base_stat(darmanitan-standard,480).
have_base_stat(maractus,461).
have_base_stat(dwebble,325).
have_base_stat(crustle,475).
have_base_stat(scraggy,348).
have_base_stat(scrafty,488).
have_base_stat(sigilyph,490).
have_base_stat(yamask,303).
have_base_stat(cofagrigus,483).
have_base_stat(tirtouga,355).
have_base_stat(carracosta,495).
have_base_stat(archen,401).
have_base_stat(archeops,567).
have_base_stat(trubbish,329).
have_base_stat(garbodor,474).
have_base_stat(zorua,330).
have_base_stat(zoroark,510).
have_base_stat(minccino,300).
have_base_stat(cinccino,470).
have_base_stat(gothita,290).
have_base_stat(gothorita,390).
have_base_stat(gothitelle,490).
have_base_stat(solosis,290).
have_base_stat(duosion,370).
have_base_stat(reuniclus,490).
have_base_stat(ducklett,305).
have_base_stat(swanna,473).
have_base_stat(vanillite,305).
have_base_stat(vanillish,395).
have_base_stat(vanilluxe,535).
have_base_stat(deerling,335).
have_base_stat(sawsbuck,475).
have_base_stat(emolga,428).
have_base_stat(karrablast,315).
have_base_stat(escavalier,495).
have_base_stat(foongus,294).
have_base_stat(amoonguss,464).
have_base_stat(frillish,335).
have_base_stat(jellicent,480).
have_base_stat(alomomola,470).
have_base_stat(joltik,319).
have_base_stat(galvantula,472).
have_base_stat(ferroseed,305).
have_base_stat(ferrothorn,489).
have_base_stat(klink,300).
have_base_stat(klang,440).
have_base_stat(klinklang,520).
have_base_stat(tynamo,275).
have_base_stat(eelektrik,405).
have_base_stat(eelektross,515).
have_base_stat(elgyem,335).
have_base_stat(beheeyem,485).
have_base_stat(litwick,275).
have_base_stat(lampent,370).
have_base_stat(chandelure,520).
have_base_stat(axew,320).
have_base_stat(fraxure,410).
have_base_stat(haxorus,540).
have_base_stat(cubchoo,305).
have_base_stat(beartic,485).
have_base_stat(cryogonal,485).
have_base_stat(shelmet,305).
have_base_stat(accelgor,495).
have_base_stat(stunfisk,471).
have_base_stat(mienfoo,350).
have_base_stat(mienshao,510).
have_base_stat(druddigon,485).
have_base_stat(golett,303).
have_base_stat(golurk,483).
have_base_stat(pawniard,340).
have_base_stat(bisharp,490).
have_base_stat(bouffalant,490).
have_base_stat(rufflet,350).
have_base_stat(braviary,510).
have_base_stat(vullaby,370).
have_base_stat(mandibuzz,510).
have_base_stat(heatmor,484).
have_base_stat(durant,484).
have_base_stat(deino,300).
have_base_stat(zweilous,420).
have_base_stat(hydreigon,600).
have_base_stat(larvesta,360).
have_base_stat(volcarona,550).
have_base_stat(cobalion,580).
have_base_stat(terrakion,580).
have_base_stat(virizion,580).
have_base_stat(tornadus-incarnate,580).
have_base_stat(thundurus-incarnate,580).
have_base_stat(reshiram,680).
have_base_stat(zekrom,680).
have_base_stat(landorus-incarnate,600).
have_base_stat(kyurem,660).
have_base_stat(keldeo-ordinary,580).
have_base_stat(meloetta-aria,600).
have_base_stat(genesect,600).
have_base_stat(chespin,313).
have_base_stat(quilladin,405).
have_base_stat(chesnaught,530).
have_base_stat(fennekin,307).
have_base_stat(braixen,409).
have_base_stat(delphox,534).
have_base_stat(froakie,315).
have_base_stat(frogadier,405).
have_base_stat(greninja,530).
have_base_stat(bunnelby,237).
have_base_stat(diggersby,423).
have_base_stat(fletchling,278).
have_base_stat(fletchinder,382).
have_base_stat(talonflame,499).
have_base_stat(scatterbug,200).
have_base_stat(spewpa,213).
have_base_stat(vivillon,411).
have_base_stat(litleo,369).
have_base_stat(pyroar,507).
have_base_stat(flabebe,303).
have_base_stat(floette,371).
have_base_stat(florges,552).
have_base_stat(skiddo,350).
have_base_stat(gogoat,531).
have_base_stat(pancham,348).
have_base_stat(pangoro,495).
have_base_stat(furfrou,472).
have_base_stat(espurr,355).
have_base_stat(meowstic-male,466).
have_base_stat(honedge,325).
have_base_stat(doublade,448).
have_base_stat(aegislash,520).
have_base_stat(spritzee,341).
have_base_stat(aromatisse,462).
have_base_stat(swirlix,341).
have_base_stat(slurpuff,480).
have_base_stat(inkay,288).
have_base_stat(malamar,482).
have_base_stat(binacle,306).
have_base_stat(barbaracle,500).
have_base_stat(skrelp,320).
have_base_stat(dragalge,494).
have_base_stat(clauncher,330).
have_base_stat(clawitzer,504).
have_base_stat(helioptile,289).
have_base_stat(heliolisk,481).
have_base_stat(tyrunt,362).
have_base_stat(tyrantrum,521).
have_base_stat(amaura,362).
have_base_stat(aurorus,521).
have_base_stat(sylveon,525).
have_base_stat(hawlucha,502).
have_base_stat(dedenne,431).
have_base_stat(carbink,500).
have_base_stat(goomy,300).
have_base_stat(sliggoo,452).
have_base_stat(goodra,600).
have_base_stat(klefki,474).
have_base_stat(phantump,309).
have_base_stat(trevenant,474).
have_base_stat(pumpkaboo-average,335).
have_base_stat(gourgeist-average,494).
have_base_stat(bergmite,307).
have_base_stat(avalugg,514).
have_base_stat(noibat,245).
have_base_stat(noivern,535).
have_base_stat(xerneas,680).
have_base_stat(yveltal,680).
have_base_stat(zygarde,600).
